"use strict"; // treat all js code as newer version

alert(3 + 3) // we are using node js not browser

console.log(3 + 3)

console.log("CK Ray")

let age = 12
console.log(typeof age) 

// Null is data type that represents the intentional absence of any object value. It is one of JavaScript's primitive values and is treated as falsy for boolean operations.

let empty = null
console.log(empty) // null
console.log(typeof empty) // object (this is a known bug in JavaScript, but it is what it is)